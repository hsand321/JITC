<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jitc">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.JITC">
        <activity android:name=".GetStartedActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.MainActivity" />
        <activity android:name=".ui.OfficeActivity" />
        <activity android:name=".ui.DesainGrafisActivity" />
        <activity android:name=".ui.WebActivity" />
        <activity android:name=".ui.DatabaseActivity" />
        <activity android:name=".ui.OsNetworkActivity" />
        <activity android:name=".ui.PemogramanActivity" />
        <activity android:name=".ui.MultimediaActivity" />
        <activity android:name=".ui.ModellingActivity" />
        <activity android:name=".ui.RequestActivity"/>

    </application>

</manifest>